# Attribution

This workflow kit is original work (MIT, © 2026 Luis Camargo — see `LICENSE`), but several
skills were **adapted from** or **influenced by** prior open-source work. The rewrites are
this project's own — one-agent economics, model portability, evidence-first verification,
human authority — but the ideas deserve credit. Each adapted skill also carries an inline
`Influence:` line naming its specific source; this file consolidates them.

None of the sources' text is redistributed verbatim; every skill here is a re-authoring. Where
a source's licence requires it, that licence is noted below.

## Sources

- **Matt Pocock — `mattpocock/skills`** (MIT, © Matt Pocock, 2026)
  Influenced: `architecture-review` (`codebase-design`, `improve-codebase-architecture`),
  `debugging` (`diagnosing-bugs`), `testing` (`tdd`), `planning` (`to-tickets`, `wayfinder`),
  `wayfinder` (`wayfinder`), `resolving-merge-conflicts` (`resolving-merge-conflicts`),
  `wiki` (the decision-record gate from `domain-modeling`), `writing-great-skills`
  (`writing-great-skills`).

- **Lauren Tan — pstack** (MIT)
  Influenced: `topology` (`arena`/`swarm`), `experience-design` (`architect`/`arena`),
  `architecture-review` (`architect`), `create-verification` (`create-verification-skill`),
  `maintain-verification` (`maintain-verification-skill`), `resume-work` (`recall`,
  `session-pickup`, `pause-safely`), `release-evidence` (`show-me-your-work`).

- **Obra Superpowers — `obra/superpowers`** (MIT, © Jesse Vincent, 2025)
  Influenced: `debugging` (`systematic-debugging`), `testing` (`test-driven-development` and
  its testing anti-patterns), `planning` (file handoffs, plan self-review), `writing-great-skills`
  (`writing-skills`, behavior-testing and failure-shaping).

- **Andrej Karpathy — "LLM Wiki" pattern** (public gist `442a6bf555914893e9891c11519de94f`)
  Influenced: `wiki` (three layers: sources / wiki / schema; three operations: ingest / query /
  lint).

- **Philipp Bakaus — `pbakaus/impeccable`** (Apache 2.0)
  Influenced: `ui-ux` / voice rules. Per Apache 2.0 §4, this notice preserves the
  attribution; no NOTICE text from that project was itself carried over.

- **`Leonxlnx/taste-skill`** (MIT) and **Anthropic's `frontend-design` skill** (no separate
  licence carried; influence only)
  Influenced: `taste` (its AI-slop catalog and render audit). The taste rules here are a
  distilled, voice-agnostic re-authoring — the third-party skills' text is not vendored.

If you recognize an influence that should be credited here and is not, please open an issue.
